# 方法流程

1. 隐式关联视角与显式对应视角分别基于同一图像和文本独立生成候选判别依据，两者不交互。
2. 训练阶段以真实标签作为候选选择和冲突消解约束；标签不是新证据，也不触发再次类别预测，得到 A0。
3. 以标签泄露、答案指示线索、模板短语和文本探针评估总体风险。诊断不承担样本级筛除。
4. LIRN 去除或中和标签信息、答案线索及结论先行表达；NOPAD 删除或自然改写模板短语，得到 A1。
5. 学生模型仅以图像与文本为输入，以 A1 和真实标签为监督目标。推理时不接收标签或教师中间依据。

